apiVersion: v1
kind: ConfigMap
metadata:
  name: donation-service-config
  labels:
    app: donation-service
  namespace: solidarytech
data:
  PORT: "8082"
  DB_HOST: "donation-db.c69c2yweoc4n.us-east-1.rds.amazonaws.com"
  DB_PORT: "5432"
  DB_NAME: "donation_db"
  AWS_REGION: "us-east-1"
  AWS_SQS_URL: "https://sqs.us-east-1.amazonaws.com/451638006344/solidary-donations"
---
apiVersion: v1
kind: Secret
metadata:
  name: donation-service-secret
  labels:
    app: donation-service
  namespace: solidarytech
type: Opaque
stringData:
  DATABASE_URL: "postgres://postgres:postgres@donation-db.c69c2yweoc4n.us-east-1.rds.amazonaws.com:5432/donation_db"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: donation-service
  labels:
    app: donation-service
  namespace: solidarytech
spec:
  replicas: 2
  revisionHistoryLimit: 3

  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1

  selector:
    matchLabels:
      app: donation-service

  template:
    metadata:
      labels:
        app: donation-service
    spec:
      serviceAccountName: donation-service-sa

      securityContext:
        runAsNonRoot: false
        seccompProfile:
          type: RuntimeDefault

      containers:
        - name: donation-service
          image: 451638006344.dkr.ecr.us-east-1.amazonaws.com/donation-service:v1.0.0
          imagePullPolicy: IfNotPresent

          ports:
            - name: http
              containerPort: 8082
              protocol: TCP

          envFrom:
            - configMapRef:
                name: donation-service-config
            - secretRef:
                name: donation-service-secret

          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"

          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL

          startupProbe:
            httpGet:
              path: /health
              port: http
            failureThreshold: 30
            periodSeconds: 5

          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3

          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 20
            periodSeconds: 20
            timeoutSeconds: 3
            failureThreshold: 3

          volumeMounts:
            - name: tmp
              mountPath: /tmp

      volumes:
        - name: tmp
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: donation-service
  labels:
    app: donation-service
  namespace: solidarytech
spec:
  type: ClusterIP
  selector:
    app: donation-service
  ports:
    - name: http
      protocol: TCP
      port: 8082
      targetPort: http
